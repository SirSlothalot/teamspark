extends layout

block content
  .container-fluid
    .row
      #coverflow01.coverflow
        #main.jumbotron
          //img#avatarImg.avatar(src='/images/user_image.png', alt='Avatar')
          img(src='data:image/png;base64,#{image.data}', style="height=150px; width=150px;")
          h1 #{person.fullname}
          h2
            i #{person.username}
    .row
      .content
        .container
          .row
              .col-sm-8.col-sm-offset-2
                  .text-left
                      h3 Bio:
                      h4 #{person.bio}
          .row
              .col-sm-4.col-sm-offset-2
                  .text-left
                      h3 Country:
                      h4 #{person.country}
                      h3 State: 
                      h4 #{person.state}
                      h3 Date of birth:
                      h4 #{person.dob}
              .col-sm-4
                  .text-left
                      h3 Skill level:
                      h4 #{person.skillLevel}
                      h3 Availability per week:
                      h4 #{person.availability}
                      //- h2 Works online: #{user.online}
                      h3 Interest:
                      h4 #{person.userInterest}
          .row
              .col-sm-8.col-sm-offset-2
                  .text-left
                    h3 Programming languages: 
                    h4 
                      - for (var i=0; i<person.programmingLanguages.length; i++)
                        - if (i != person.programmingLanguages.length - 1)
                          span= person.programmingLanguages[i]+", "
                        - else
                          span= person.programmingLanguages[i]
                    
                    
          .row
              .col-sm-8.col-sm-offset-2
                  .text-left
                      h3 Accounts:
                      h4 
                        - for (var i=0; i<person.accounts.length; i++)
                          - if (i != person.accounts.length - 1)
                            span= person.accounts[i]+", "
                          - else
                            span= person.accounts[i]
          .row
              button.btn.btn-default Delete Account
